# Dotfiles chooser

React app for choosing Ubuntu setup options and copying an install command.

## Develop

```sh
npm install
npm run dev
```

## Build

```sh
npm run build
```

The app imports package lists from `../Ubuntu/packages/*.txt` so the installer
and chooser use the same package names.
